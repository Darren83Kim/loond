name: loond
description: 로온드 — 수원 기회(신청·즐기기·발견)를 한곳에서.
publish_to: "none"
version: 0.1.4+5

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.1
  web: ^1.1.1
  intl: ^0.19.0
  google_mobile_ads: ^9.0.0
  shared_preferences: ^2.5.5
  share_plus: ^13.3.0
  http: ^1.6.0
  path_provider: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/opportunities.json
    - assets/data/opportunities_sample.json
    - assets/images/
